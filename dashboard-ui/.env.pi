# Pi deployment - Convex reached through the Python server's HTTPS proxy.
# A bare http://host:3210 URL is mixed content on the HTTPS dashboard and the
# browser silently blocks it, leaving the app stuck on "Connecting...".
NEXT_PUBLIC_CONVEX_URL=https://nightwatch.local/convex
