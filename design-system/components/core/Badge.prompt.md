Inline status chip — sensor states, "Required"/"Optional" tags, and the violet SIM marker on simulated data.

```jsx
<Badge tone="normal">online</Badge>
<Badge tone="simulated" uppercase>Sim</Badge>
```

Tones: neutral, accent, normal, warning, critical, simulated. `uppercase` switches to the 10px tracked caps treatment.
