Thin rounded progress track on the muted surface.

```jsx
<Progress value={62} tone="accent" />
```

Tones follow the status palette; use `normal`/`warning`/`critical` when the bar itself reports sensor health.
