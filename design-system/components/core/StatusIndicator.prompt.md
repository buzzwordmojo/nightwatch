The dot that reports whether something is alive — used in the header, on detector rows, and beside sensor names.

```jsx
<StatusIndicator status="normal" label="System" />
<StatusIndicator status="offline" size="sm" />
```

Green = online/normal (pulses), amber = warning/degraded/stale, red = error/alert/critical/offline, grey = unknown.
