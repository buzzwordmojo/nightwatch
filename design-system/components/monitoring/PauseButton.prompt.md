Header control that suspends alerting for a set window; also the reference dropdown pattern for this system (4px-padded panel, 6px radius, muted hover rows).

```jsx
<PauseButton isPaused={false} onPause={(m) => pause(m)} pauseIcon={<Pause size={16}/>} />
```
